---
description: 每次新会话先读 PROJECT.md，回答结束后按需更新
alwaysApply: true
---

# 项目会话上下文（PROJECT.md）

本项目的**活文档**位于 `.cursor/PROJECT.md`，用于跨会话延续上下文。设计细节仍以 `.cursor/design/` 为准；本文件只记录当前状态、近期变更与待办。

## 会话开始

每次**新窗口 / 新对话**的第一件事：

1. 读取 `.cursor/PROJECT.md`
2. 结合用户问题与文档中的「当前焦点」「待办」「已知决策」作答
3. 若文档与代码明显不一致，以代码为准，并在本次回答末尾更新文档

## 回答结束后

完成回答后，**如有必要**更新 `.cursor/PROJECT.md`：

### 应更新

- 完成了里程碑、功能或重要 bug 修复
- 做出了会影响后续开发的决策（架构、命名、范围取舍）
- 新增/变更待办、阻塞项、已知问题
- 用户明确了新的优先级或约束

### 不必更新

- 纯解释性问题，未改变项目状态
- 一次性操作且无后续影响
- 文档已准确反映现状

## 维护原则

- 保持精简：目标 80 行以内，删去过时条目
- 只写**结论与状态**，不写长篇过程；细节链到 `.cursor/design/` 或代码路径
- 更新「最后更新」日期与「近期变更」摘要（最近 3～5 条）
- 不要写入密钥、令牌等敏感信息
